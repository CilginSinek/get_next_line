#include "get_next_line.h"

// char *ft_realloc(char *st1, size_t new_size)
// {
//     char *new_st;

//     new_st = malloc(new_size);
//     if (!new_st)
//         return (NULL);
//     ft_memcpy(new_st, st1, ft_strlen(st1));
//     free(st1);
//     return (new_st);
// }
